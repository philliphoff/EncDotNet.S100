#### Agent guidance

Use this as the low-cost discovery step before rendering or querying an
unfamiliar dataset. It reports the detected specification, declared and
supported editions, headless-render capability, display modes, and indexed UTC
time steps where available. The command is human-readable only.
