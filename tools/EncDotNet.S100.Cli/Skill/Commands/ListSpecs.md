#### Agent guidance

Use this to discover whether the installed build knows a product specification
and exposes a headless portrayal path. Coverage products support headless
rendering only for gridded datasets; a listed product does not imply every data
coding format is renderable.
